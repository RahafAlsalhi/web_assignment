<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>hello <%- title %></h1>
    <% if (seconds % 2 === 0) { %>
    <p>no item to preview</p>
    <% } else { %> <% for (let x = 0; x < items.length; x++) { %> <%= items[x]
    %><br />
    <% } %> <% } %>
  </body>
</html>
